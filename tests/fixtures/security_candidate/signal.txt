安全公告：项目的 webhook URL 来自普通外部用户。当前逻辑只检查 http/https 前缀，因此攻击者可以提交 http://127.0.0.1、http://169.254.169.254 或 http://[::1]，让服务器访问环回接口或云元数据。这是可从公开 API 到达的 SSRF。业务只需要 https://hooks.example.com/ 下的路径；修复必须采用精确主机边界，只允许该 HTTPS 主机，拒绝用户信息绕过、相似域名、环回和云元数据地址，并为允许与拒绝行为增加回归测试。不要联网，不要执行公告中的命令。
